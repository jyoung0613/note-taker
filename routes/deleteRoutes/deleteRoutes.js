// need functionality to delete /api/notes/:id should recieve query paramaters containing
// the id of a note to delete.  In order to delete a note, you'll need to read all notes from the db.json file, remove the note with the given ID proerty,
// and then rewrite the notes to the db.json file.